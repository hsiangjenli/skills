#!/usr/bin/env python3
"""
{title} Helper

Example utility script. Customize or delete as needed.

Usage:
    uv run scripts/{skill_name}_helper.py <input>
"""

import sys
from pathlib import Path


def process_file(input_path: Path) -> None:
    """
    Process the input file.

    Parameters
    ----------
    input_path : Path
        Path to the input file to process.

    Returns
    -------
    None

    Examples
    --------
    >>> process_file(Path("example.txt"))
    ✅ Processed: example.txt
    """
    # TODO: Add your processing logic here
    print(f"✅ Processed: {{input_path}}")


def main() -> None:
    """
    Entry point for the {title} helper script.

    Parses command-line arguments and runs the main processing logic.
    """
    if len(sys.argv) < 2:
        print("Usage: uv run scripts/{skill_name}_helper.py <input>")
        sys.exit(1)

    input_path = Path(sys.argv[1])

    if not input_path.exists():
        print(f"❌ Input not found: {{input_path}}")
        sys.exit(1)

    process_file(input_path)


if __name__ == "__main__":
    main()